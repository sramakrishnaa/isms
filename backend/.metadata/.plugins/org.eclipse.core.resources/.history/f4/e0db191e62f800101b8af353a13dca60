spring:
  application:
    name: identity-service
  datasource:
    password: krishna
    username: root
    url: jdbc://mysql:localhost:3036/auth_db?createIfDatabaseNotExist=true

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update 

server:
  port: 8080
  